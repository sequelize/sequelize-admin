@import 'nib';

body {
  padding-top: 60px;
  padding-bottom: 40px;
}

.sidebar-nav {
  padding: 9px 0;
}

.dao_factories {
  &.index {
    .actions {
      span {
        display: block;
        cursor: pointer;
      }
    }
  }
}
