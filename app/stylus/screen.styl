@import 'nib';

body {
  padding-top: 60px;
  padding-bottom: 40px;
}

.sidebar-nav {
  padding: 9px 0;
}

.daos {
  &.index {
    .actions {
      span {
        display: block;
        cursor: pointer;
      }
    }
  }
}

.modal {
  width: 800px;
  margin-left: -400px;
  top: 50px;
  margin-top: 0px;
}
